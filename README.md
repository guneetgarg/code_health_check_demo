# Odd/Even Checker

Simple Node.js script to check whether an integer is odd or even.

Usage:

```powershell
node odd_even.js 42
```

Example:

```powershell
node odd_even.js 7
# Outputs: 7 is Odd
```

API:

- `isEven(n)` — returns `true` if `n` is even, `false` otherwise.
